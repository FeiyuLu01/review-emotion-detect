// Placeholder for analysis-related API calls
// Later you can implement:
//  - POST /api/analyze -> returns emotion distribution, key sentences, tips, etc.
import http from '@/api/http'

// Example stub to keep structure clear (not used yet)
export function analyzeReview(payload) {
  // return http.post('/api/analyze', payload)
  return Promise.resolve({
    // This is only a stub response for future integration
  })
}